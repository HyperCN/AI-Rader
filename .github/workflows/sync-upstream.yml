name: 'Sync Upstream Template'
on:
  schedule:
    # 每周日UTC时间23:30自动运行（北京时间周一上午7:30）
    - cron: '30 23 * * 0'
  workflow_dispatch: # 允许在GitHub网页手动点击运行
  push:
    branches: [ master ]
    paths:
      - '.github/workflows/sync.yml' # 当此工作流文件本身更新时也运行

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Target
        uses: actions/checkout@v3
        with:
          ref: master
          token: ${{ secrets.PAT_TOKEN }}

      - name: Sync Template
        uses: AndreasAugustin/actions-template-sync@v1
        with:
          source_repo: sansan0/TrendRadar
          upstream_branch: master
          target_branch: master
          github_token: ${{ secrets.PAT_TOKEN }}
          
          # PR 相关设置
          pr_title: '🚀 chore: 同步上游模板更新'
          pr_body: '自动化工具已检测并同步来自上游仓库 [sansan0/TrendRadar](https://github.com/sansan0/TrendRadar) 的更新。'
          pr_label: 'automated-sync'
          
          # 关键：排除你已修改的配置文件，防止被覆盖
          excluded_files: |
            config.yml
            config.yaml
            .env
            .env.*
            README.md
          
          # 同步前执行一次硬重置，确保状态干净（对首次同步很有效）
          hard_reset: false # 设为 true 可强制完全同步，但会丢弃本地未推送提交
